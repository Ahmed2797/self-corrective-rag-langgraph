# self-corrective-rag-langgraph
The codebase implements a Self-Corrective Agentic RAG Pipeline (LangGraph) designed to answer user queries with high precision and zero hallucinations. Unlike standard RAG systems that execute a single retrieval-generation pass, this architecture introduces a recursive loop that verifies, evaluates, and corrects its own outputs dynamically.
